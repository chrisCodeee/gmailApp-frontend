import styled from "styled-components";

const AppWrapper = styled.div`
	position: relative;
	width: 100vw;

	@media screen and (min-width: 1900px) {
		// width: 80vw;
		// margin: auto;
	}
`;

export { AppWrapper };
